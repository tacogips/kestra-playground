# kestra-playground

A Python project

## Requirements

- Python 3.12 or newer
- `uv`

## Quick Start

```bash
uv sync --dev
uv run python -m kestra-playground
uv run pytest
```

## Common Commands

```bash
task sync
task run
task test
task lint
task typecheck
task fmt
task build
```
